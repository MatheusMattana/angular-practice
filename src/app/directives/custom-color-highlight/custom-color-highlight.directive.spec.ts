import { CustomColorHighlightDirective } from './custom-color-highlight.directive';

describe('CustomColorHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomColorHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
